/**
 * @deprecated Import from "@/lib/ai" instead.
 * Kept so older imports keep working — switch providers via env only.
 */
export { getModel, getActiveProvider, isLlmErrorMissingKey } from "@/lib/ai";
